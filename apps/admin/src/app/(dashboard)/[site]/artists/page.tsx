// 골격 placeholder — 아티스트 CRUD는 다음 단계 엔티티 에이전트가 이 디렉토리를 채운다.
export default function ArtistsPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">아티스트</h1>
      <p className="text-muted-foreground text-sm">다음 단계에서 구현됩니다.</p>
    </div>
  );
}
